/*
** EPITECH PROJECT, 2022
** B-YEP-400-PAR-4-1-indiestudio-martin.vanaud
** File description:
** loadSettings
*/

#include <unordered_map>

#include "tools.hpp"

SettingsParams loadSettings(std::string const &filepath)
{
    // std::vector<std::string> allParams = strToWordArr(readFile(filepath, "\n"), '\n');
    // std::unordered_map<std::string, std::string> splitParams;
    SettingsParams params;

    // for (auto oneParam : allParams) {
        // std::vector<std::string> tmp = strToWordArr(oneParam, '=');

        // splitParams.emplace(tmp.at(0), tmp.at(1));
    // }
    return params;
}
